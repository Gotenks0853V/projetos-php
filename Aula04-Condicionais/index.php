<?php
    //Estruturas Condicionais
    $nome = 'Alessandro';

    // Operadores de Comparação

    // == (igual)
    // != (Diferente)

    // > (Maior que)
    // < (Menor que)
    // >= (Maior ou igual a)
    // <= (Menor ou igual a)

    if ($nome == 'Alessandro') {
        echo 'Olá Alessandro';
    }
?>